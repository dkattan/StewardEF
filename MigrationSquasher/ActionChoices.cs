namespace MigrationSquasher;

public static class ActionChoices
{
    public const string CreateAggregateFiles = "Create aggregate files";
    public const string SquashMigrations = "Squash migrations";
}